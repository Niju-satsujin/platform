# trustctl (Week 01 starter)

This starter is intentionally blank.
You build the real CLI from zero through the lessons.

## Build with CMake (creates `a.out`)
make build

## Run tests (always deterministic)
make test

## Direct CMake commands
cmake -S . -B build
cmake --build build
./a.out
